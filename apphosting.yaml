runConfig:
  minInstances: 0
  # Try to preserve existing IAM configuration
  preserveExistingTraffic: true
env:
  - variable: DATABASE_URL
    secret: client-database-url
  - variable: FIREBASE_ADMIN_SDK_KEY
    secret: firebase-admin-sdk-key
    availability:
      - BUILD
      - RUNTIME
